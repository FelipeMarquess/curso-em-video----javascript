function soma (n1 = 0, n2 = 0){ //passando valores pré-definidos para os parametros, caso algum deles não sejam digitados, o valor nesse caso será considerado 0!
    return (n1 + n2);
}

let resu = soma(10, 20);
console.log(soma(7))